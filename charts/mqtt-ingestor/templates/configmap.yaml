apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mqtt-ingestor.fullname" . }}-config
  labels:
    {{- include "mqtt-ingestor.labels" . | nindent 4 }}
data:
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}