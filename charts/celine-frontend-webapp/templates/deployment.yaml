apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "celine-frontend-webapp.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "celine-frontend-webapp.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "celine-frontend-webapp.fullname" . }}
    spec:
      containers:
        - name: celine-frontend-webapp
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
