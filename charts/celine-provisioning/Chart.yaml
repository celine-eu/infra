apiVersion: v2
name: celine-provisioning
description: >
  CELINE participant provisioning service (celine.provisioning.main:create_app).
  Runs the ghcr.io/celine-eu/celine-policies image with its own entrypoint.
  Internal only: holds realm-wide Keycloak administration (ADR-0007 in
  celine-policies), so it has no ingress and no api-gateway route.
type: application
version: 0.1.0
appVersion: "latest"
dependencies:
  - name: celine-services
    version: "0.1.0"
    repository: file://../celine-services
